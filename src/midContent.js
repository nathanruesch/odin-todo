import projects from './projects';
import todos from './todos';

const midContent = () => {
    const parentDiv = document.createElement("div");
    parentDiv.classList.add("mid-content-div");

    
};

export default midContent;